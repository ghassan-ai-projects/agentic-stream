# Documentation review record — 2026-08-17

This record is the reproducible handoff for the documentation reorganization.
It covers the curated `documentation/` tree, root open-source entrypoints, the
working archive boundary, and volatile claims against the current code.

## Independent reviews

- Completeness and open-source readiness: no P0/P1 findings after correction.
- Code alignment: no P0 findings; the review drove corrections to the
  predictive-maintenance claim, CLI configuration wording, worker socket path,
  and capability-negotiation language.
- Writing, navigation, and diagrams: no P0/P1 findings after correction; the
  review drove section landing pages, page-contract introductions, text
  equivalents for Mermaid diagrams, and a structural Mermaid fence check.

## Findings closed in this pass

- The one-event predictive-maintenance fixture is documented as stream-plane
  coverage; synthetic tests are identified as the evidence for later action
  stages.
- Schema-visible but incompletely enforced policy modes and retention/telemetry
  fields are disclosed as current limitations and release gaps.
- Quarantine redrive, approval resolution, reconciliation, and explainability
  inspection are labeled internal capabilities because no public CLI/HTTP
  surface currently exposes them.
- The CLI reference enumerates the `serve` worker/model flags and identifies
  certificate/key values as PEM file paths.
- The public/archive split is explicit for current example fixtures and dated
  implementation audits.
- Root support and conduct routes are actionable for the current GitHub host.

## Validation evidence

The following checks passed in this checkout:

```text
python3 scripts/check-documentation.py
make docs-check
make build
git diff --check
agentic-stream validate docs/design/examples/predictive-maintenance.situation.yaml
agentic-stream run --db <fresh-path> --spec <fixture> --trace <opening-trace>
agentic-stream run-live --db <fresh-path> --spec <fixture> --trace <opening-trace>
```

The documentation checker validates local links, reachability, non-stub pages,
machine-local/credential leakage, root entrypoints, CLI/API/environment
surfaces, the ten invariants, release-status JSON, and Mermaid fence balance.

## Explicit residual limits

- Mermaid source is structurally checked; a renderer is not installed in this
  environment, so visual rendering remains a maintainer review step.
- The repository remains an unreleased development snapshot. Backup/restore,
  disk-full, long-running soak, external-effector, and release-artifact
  qualification remain release blockers in `release-status.json`.
- The full Go suite still reports the pre-existing modified domain-registry
  golden mismatch and a separate-process worker socket failure in this
  environment. `go vet` is blocked by the sandbox's Go build-cache permission.
  These are recorded here rather than hidden by the documentation gate.

## Next reads

- [Quality governance](quality.md)
- [Release status](release-status.json)
- [Limitations](../overview/limitations.md)
