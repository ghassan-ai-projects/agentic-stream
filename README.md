# agentic-stream
Streaming-native agent runtime — continuously converts unbounded evidence into durable, versioned Situations; bounded agent episodes behind a deterministic scheduler and policy plane
